# Trellis Feedback — OrderManagement

> Generated by AI while building OrderManagement on 2025-07-16.
> Trellis version: 3.0.0-alpha.137
> AI model: Claude claude-opus-4.6

## Summary

Building the Order Management System with Trellis was efficient overall. The framework's source generators, value objects, and Result<T> pattern provided strong guardrails. A few friction points arose around composite ValueObject equality, Maybe<T> property access in specifications, and the imperative workarounds needed for Result monadic composition.

## Friction Points

### FP-1: RequiredString<T> does not implement IComparable — composite ValueObject equality breaks
- **Category:** Workaround Required
- **Severity:** Medium (slowed progress)
- **Context:** Creating a composite `ShippingAddress : ValueObject` with `GetEqualityComponents()` that yields `Street`, `City`, `State`, `PostalCode`, `Country` (all `RequiredString<T>` types).
- **What happened:** Casting `RequiredString<T>` to `IComparable` throws `InvalidCastException` at runtime. The `GetEqualityComponents()` signature requires `IEnumerable<IComparable?>`, but `RequiredString<T>` does not implement `IComparable`.
- **Workaround used:** Yielded the underlying `.Value` (string) instead:
  ```csharp
  protected override IEnumerable<IComparable?> GetEqualityComponents()
  {
      yield return Street.Value;
      yield return City.Value;
      // ...
  }
  ```
- **Suggested improvement:** Either have `RequiredString<T>` implement `IComparable` or provide clear documentation/example for composite ValueObjects that contain Trellis value object properties.

### FP-2: TRLS006 treats Maybe<T>.Value access as error in specification expressions
- **Category:** Workaround Required
- **Severity:** Medium (slowed progress)
- **Context:** Writing `OverdueOrderSpecification` that checks `order.SubmittedAt.Value < cutoff` where `SubmittedAt` is `Maybe<DateTime>`.
- **What happened:** The TRLS006 analyzer flags `Maybe<T>.Value` access as an error, even when preceded by `HasValue` checks in the same expression.
- **Workaround used:** Used `GetValueOrDefault(DateTime.MaxValue)` instead of `.Value`:
  ```csharp
  order.SubmittedAt.GetValueOrDefault(DateTime.MaxValue) < cutoff
  ```
- **Suggested improvement:** Either make TRLS006 flow-sensitive (recognize `.HasValue` guard) or provide a `Maybe<T>.Match()` or `Maybe<T>.Map()` that is analyzer-friendly for specification expressions.

### FP-3: Result monadic composition (ParallelAsync, BindAsync) is difficult to use correctly
- **Category:** Ambiguous API
- **Severity:** Medium (slowed progress)
- **Context:** In `CreateDraftOrderCommand` handler, trying to fetch Customer and Products in parallel using `Result.ParallelAsync`, then compose results with `BindAsync`.
- **What happened:** `Result.ParallelAsync` with async lambdas inside non-async delegates caused compilation errors. `BindAsync` had ambiguity between `Task` and `ValueTask` overloads. Required multiple rewrites.
- **Workaround used:** Rewrote to imperative style with early `if (!result.IsSuccess)` returns instead of fluent chaining.
- **Suggested improvement:** Provide clearer documentation or examples for common composition patterns. Consider providing a `Result.All(task1, task2)` overload that accepts `Task<Result<T>>` directly.

### FP-4: TRLS001 requires discarding Results even in fire-and-forget scenarios
- **Category:** Minor Inconvenience
- **Severity:** Low (minor inconvenience)
- **Context:** In `CancelOrderCommand` handler, calling `product.ReleaseStock()` and `repo.SaveAsync()` where the result is intentionally not needed (best-effort stock release during cancellation).
- **What happened:** TRLS001 requires every `Result` to be consumed. Had to add `_ =` discards for intentionally ignored results.
- **Workaround used:** `_ = product.ReleaseStock(...)` and `_ = await repo.SaveAsync(...)`.
- **Suggested improvement:** This is arguably correct behavior. Perhaps document a pattern for fire-and-forget Result operations, or provide a `.Forget()` extension.

### FP-5: LineItem entity constructor visibility
- **Category:** Workaround Required
- **Severity:** Low (minor inconvenience)
- **Context:** Application layer handler needed to create `LineItem` instances, but the Trellis entity generator made the constructor `internal`.
- **What happened:** Application project couldn't call the generated internal constructor.
- **Workaround used:** Added a `public static LineItem Create(...)` factory method on `LineItem`.
- **Suggested improvement:** Document the recommended pattern for creating entities from outside the Domain assembly, or make entity constructors public by default.

## What Worked Well

- **Source generators** — Zero boilerplate for value objects, aggregates, and entities. `RequiredString<T>`, `RequiredGuid<T>`, `RequiredInt<T>` with attribute-driven validation were excellent.
- **LazyStateMachine<TState, TTrigger>** — Clean state machine integration with EF Core. `FireResult()` returning `Result<TState>` instead of throwing fits perfectly with the Result pattern.
- **SaveChangesResultUnitAsync** — Automatic conflict detection (duplicate key → ConflictError) eliminated manual try/catch around EF Core saves.
- **ApplyTrellisConventions** — Automatic EF Core mapping for all scalar value objects and Money. Only needed explicit config for the composite ShippingAddress.
- **HasTrellisIndex** — Properly resolves Maybe<T> backing fields for database indexes.
- **Authorization pipeline** — `IAuthorize`, `IAuthorizeResource<T>`, and `ResourceLoaderById` provided clean separation of authorization from business logic.
- **AddDevelopmentActorProvider** with `X-Test-Actor` header — Made integration testing with different actors/permissions trivial.
- **API versioning** via namespace convention — Zero configuration, just namespace the controller correctly.
- **Testing infrastructure** — `FakeRepository<T, TId>`, `TestActorProvider`, `WithActor()` scope, and `BeSuccess()/BeFailure()/BeFailureOfType<T>()` FluentAssertions extensions were productive.
- **TRLS003 analyzer** — Catches unchecked `.Value` access on Result, preventing bugs. Easy to suppress in test files with `#pragma warning disable`.

## Suggested New Features

### SF-1: Composite ValueObject source generator
- **Use case:** When creating composite value objects like ShippingAddress that contain multiple Trellis value object properties.
- **Proposed API:**
  ```csharp
  [CompositeValueObject]
  public partial class ShippingAddress
  {
      public Street Street { get; }
      public City City { get; }
      // GetEqualityComponents() auto-generated
  }
  ```

### SF-2: Result.AllAsync helper for parallel fetch patterns
- **Use case:** Common pattern of fetching multiple entities in parallel and failing fast if any is not found.
- **Proposed API:**
  ```csharp
  var (customer, products) = await Result.AllAsync(
      customerRepo.FindByIdAsync(customerId, ct).ToResultAsync("Customer not found"),
      productRepo.GetByIdsAsync(productIds, ct));
  ```

## Copilot Instructions Feedback

- The instructions on composite ValueObjects (`GetEqualityComponents`) should note that Trellis value object types don't implement `IComparable` and that `.Value` should be yielded instead of the wrapper type.
- The `Result.ParallelAsync` section could benefit from a concrete example showing the correct async lambda syntax, as the signature is tricky to get right.
- The `Maybe<T>` section should document the `GetValueOrDefault()` workaround for TRLS006 in specification expressions.
- The guidance on when to use `LazyStateMachine` vs `StateMachine` was clear and saved debugging time — good documentation.

## Part 2 Addendum — Order Returns Feature

> Added after implementing the Returns feature (Step 8) on the existing codebase.

### Additional Friction Points

None — the Returns feature followed all established patterns cleanly:

- Adding `Returned` to `OrderStatus` and extending the state machine was straightforward. The `LazyStateMachine` pattern supported modification without breaking existing transitions.
- `ReturnReason` value object used the same `RequiredString<T>` pattern as other VOs — source generator handled everything.
- `DeliveredAt` and `ReturnedAt` as `Maybe<DateTime>` worked with no additional EF Core configuration — `ApplyTrellisConventions` handled them automatically.
- Stock release reused the same `Product.ReleaseStock()` method from the cancel flow.
- `TimeProvider` injection for the 30-day window check was clean — optional parameter defaulting to `TimeProvider.System`.
- All 81 existing tests continued to pass with zero regressions.

### What This Validates

The architecture built in Part 1 genuinely supports incremental change. No scaffolding had to be modified, no patterns had to be bent, and no workarounds were needed. The Trellis conventions (state machine, value objects, authorization pipeline, CQRS) held up under modification.
